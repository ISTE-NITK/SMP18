1. The duplicate key refers to the most recent value. No error is thrown.

2. Error is thrown as set is an unordered data structure. So it's elements cannot be indexed.

3. Error is thrown as tuple doesnt have a function called pop(), as tuples are supposed to be immutable.

4.  {'h' , 'o' , 'c'}

5.
def fibb(n):
    a = 0
    b =1
    print(a, end = " ")
    if n>1:
        print(b,end = " ")
        if n>2:
            for _ in range(3,n+1):
                c = a+b
                print(c,end = " ")
                a = b
                b = c
                
6. 
sqrtx = lambda x: math.sqrt(x)

7. 
from collections import OrderedDict
def sortDict(a):
#a  = {1:3, 7:1, 2:4, 5:2}
l = sorted(a, key = a.get)
b = OrderedDict()
for i in l:
    b[i] = a[i]
a = b
print(a)
