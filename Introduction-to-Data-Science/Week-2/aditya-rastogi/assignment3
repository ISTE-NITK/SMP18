1.
reference to latest usage is preferred, no error.
2.
error message as set elements are unordered
3.
tuple does not support pop command, error message
4.
{c,h,o} in some random order.
5.
def fib(n):
	if n is 1:
		print ("0")
	if n is 2:
		print ("0 1")
	if n>2:
		a=0
		b=1
		print("0 1",end=" ")
		for i in range(3,n+1):
			c = a+b
			print (c,end=" ")
			a = b
			b = c
 6.
 square_root= lambda n: math.sqrt(n)
 7.
 from collections import OrderedDict
def sorting(dict):
	dic = sorted(dict,key=dict.get)
	ordict={}	
	for i in dic:
		ordict[i]=dict[i]
	dict=ordict
	print(dict)
